package four;

/*
 * 求矩阵中和最大的子矩阵.
 * 还没想到怎么做，觉得代码应该蛮长。
 * 有人写出来的话，可以分享下
 */
public class MaxSubMatrix {
	static int maxSubMatrix(int[][] array) {
		int row = array.length;
		int column = array[0].length;
		int max = array[0][0];
		int sum = array[0][0];
		for (int i = 0; i < row; i++) {
			for (int j = 0; j < column; j++) {

			}
		}
		return max;
	}

}
